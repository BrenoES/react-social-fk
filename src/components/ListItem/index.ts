import styled from 'styled-components';

const ListItem = styled.li`
  color: #000;
  padding: 1rem;
  margin: 0.75rem;
  list-style: none;
  border-radius: 14px;
  display: flex;
  align-items: center;
  box-shadow: var(--tw-ring-offset-shadow, 0 0 #0000), var(--tw-ring-shadow, 0 0 #0000), 0 1px 3px 0 #0000001a,
    0 1px 2px 0 #0000000f;
`;

export default ListItem;
